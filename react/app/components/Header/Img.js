import styled from 'styled-components';

import NormalImg from 'components/Img';

const Img = styled(NormalImg)`
  width: auto;
  height: 89px;
  margin: 0 auto;
  display: block;
`;

export default Img;
